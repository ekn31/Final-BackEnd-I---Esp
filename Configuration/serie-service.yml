server:
  port: ${port:0}

spring:
  application:
    name: serie-service
  spring:
  mongodb:
    embedded:
      version: 4.0.2
  data:
    mongodb:
      database: test
      port: 27017


message: hello from serice-service

management:
  endpoints:
    web:
      exposure:
        include: '*'
